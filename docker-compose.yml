services:
  ask-ai-server:
    build: .
    env_file: .env
    environment:
      PORT: 3000
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - sessions:/data/sessions
    restart: unless-stopped

volumes:
  sessions:
